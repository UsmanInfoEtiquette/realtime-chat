import Button from '@/components/ui/Button'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <Button size='default'>Login </Button>
}

export default page